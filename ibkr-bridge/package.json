{
  "name": "alyinvest-ibkr-bridge",
  "version": "1.0.0",
  "description": "Local bridge exposing a read-only /positions endpoint backed by a logged-in IBKR Client Portal Gateway session",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
